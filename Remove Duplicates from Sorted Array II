Given an integer array nums sorted in non-decreasing order, remove some duplicates in-place such that each unique element appears at most twice. The relative order of the elements should be kept the same.

Since it is impossible to change the length of the array in some languages, you must instead have the result be placed in the first part of the array nums. More formally, if there are k elements after removing the duplicates, then the first k elements of nums should hold the final result. It does not matter what you leave beyond the first k elements.

Return k after placing the final result in the first k slots of nums.

Do not allocate extra space for another array. You must do this by modifying the input array in-place with O(1) extra memory.

Custom Judge:

The judge will test your solution with the following code:

int[] nums = [...]; // Input array
int[] expectedNums = [...]; // The expected answer with correct length

int k = removeDuplicates(nums); // Calls your implementation

assert k == expectedNums.length;
for (int i = 0; i < k; i++) {
    assert nums[i] == expectedNums[i];
}
If all assertions pass, then your solution will be accepted.

Constraints:

1 <= nums.length <= 3 * 104
-104 <= nums[i] <= 104
nums is sorted in non-decreasing order.



class Solution {
    public int removeDuplicates(int[] nums) {

        // ✅ If array has 2 or fewer elements,
        // no number can appear more than twice
        if (nums.length <= 2) return nums.length;

        // ✅ k = index where we will place the next valid element
        // First two elements are always allowed
        int k = 2;

        // ✅ Start checking from the 3rd element (index 2)
        for (int i = 2; i < nums.length; i++) {

            /*
             ✅ Condition to allow nums[i]:

             nums[k-2] means the element placed 2 positions before.

             If nums[i] == nums[k-2],
             then nums[i] is the 3rd (or more) occurrence → NOT allowed.

             If nums[i] != nums[k-2],
             then it is either a new number or only the 2nd occurrence → allowed.
            */
            if (nums[i] != nums[k - 2]) {

                // ✅ Place the valid element at index k
                nums[k] = nums[i];

                // ✅ Move k forward for the next valid insertion
                k++;
            }
        }

        // ✅ k is the number of valid elements after removing extra duplicates
        return k;
    }
}
